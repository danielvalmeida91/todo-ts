import styled from 'styled-components'

export const Container = styled.button`
    padding: 1rem;
    border: none;
    border-radius: 8px;

    display: flex;
    align-items: center;
    gap: 8px;
`